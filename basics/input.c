#include<stdio.h>
int main(){
    // first we normaly declare a variable
    float x;
    printf("enter a num :");
    scanf("%f",&x);// we used & :- this is address of x in storage 
   printf ("%f",x);
    return 0;
}