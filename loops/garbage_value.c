#include<stdio.h>
int main(){
    int i=0;
    do{
        printf("the value of i is : %d\n");
    }
    while(i<=10);
    /*
    ab output mai jo values ayengi ajeeb si those are garbage value
    */
    return 0;
}