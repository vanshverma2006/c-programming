#include<stdio.h>
int main(){
    int x=0;
    while(x<101){
        printf("hello\n");
        x+=1;// wwe can also use x=x+1 or x++ same thing 
    }
    // while ke andar agar condition true hai to block of code execute hoga 

    
    return 0;
}