#include<stdio.h>
int main(){
    int array[10];// we can store 10 elements in it 
    array[0]=1;//starting from 0 upto 9
    array[1]=2;
    printf("value of array 0 and 1 is :: %d\t%d",array[0],array[1]);


    return 0;
}