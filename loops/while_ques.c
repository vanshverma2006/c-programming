#include<stdio.h>
int main(){
    /*
    Quick Quiz: Write a program to print natural numbers from 10 to 20 when initial loop
    */
    int i=0;
    while(i<=20){
        if(i>10)
            printf("the num is i %d\n",i);
        i++;
    }
    return 0;
}